
public class Driver {
 
  
  g.setColor(new Color GREEN);
  g.drawRectangle(0,0,816,15840);
  int[][] Map = new int[17][33];
  
Rectangle crate = new Rectangle(crates.getX(),crates.getY(),crates.getWidth(),crates.getHeight());
  
  
  
  
}
